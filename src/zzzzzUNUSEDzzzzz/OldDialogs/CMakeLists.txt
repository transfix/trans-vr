CMAKE_MINIMUM_REQUIRED (VERSION 2.6)

SetupQt()

FILE(GLOB SOURCE_FILES *.cpp)
FILE(GLOB INCLUDE_FILES ../../inc/OldDialogs/*.[Hh])

FILE(GLOB MOC_HEADERS ../../inc/OldDialogs/*.Qt3.h)

SOURCE_GROUP("Source Files" FILES ${SOURCE_FILES})
SOURCE_GROUP("Header Files" FILES ${HEADER_FILES})

INCLUDE_DIRECTORIES(
  ../../inc
)

QT4_WRAP_CPP(MOC_SOURCES ${MOC_HEADERS})


ADD_LIBRARY (OldDialogs STATIC ${SOURCE_FILES} ${INCLUDE_FILES} ${MOC_SOURCES} ${MOC_HEADERS})
